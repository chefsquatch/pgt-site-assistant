# Pinned to the versions this assistant was built and proven against.
anthropic==0.69.0
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-dotenv==1.0.1
# httpx: FastAPI TestClient AND the Resend delivery call for the contact form.
httpx==0.28.1
# EmailStr validation for the contact endpoint (server-side email-format gate).
email-validator==2.2.0
